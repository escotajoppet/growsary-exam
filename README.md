# Growsary Exam User Interface
The UI uses NuxtJS framework for SPA rendering and state management feature.

## Build Setup

``` bash
# install dependencies
$ npm install

# serve with hot reload at localhost:3000
$ npm run dev
```
